#!/bin/bash

# Day05: 日志分析与归档实战 (Log Analysis & Archiving)

# 场景描述：
# 你的任务是模拟一个线上日志处理流程。
# 你需要先生成一些模拟的 Web 服务器访问日志，然后从中提取关键信息（如 IP、错误 URL），最后按日期归档。

# ==============================================================================
# 第一阶段：环境准备与数据模拟
# ==============================================================================

# 1. 创建工作目录 workspace/web_logs，并进入该目录
mkdir workspace && mkdir workspace/web_logs && cd workspace/web_logs
# TODO: 写出命令

# 2. 生成模拟日志文件 access.log
# 内容包含：时间、IP、请求方法、URL、状态码
# 请执行以下命令生成数据（直接复制执行即可）：
cat > access.log << 'EOF'
[2023-01-01 10:00:01] 192.168.1.10 GET /index.html 200
[2023-01-01 10:00:05] 192.168.1.11 POST /login 200
[2023-01-01 10:01:20] 10.0.0.5 GET /api/user 404
[2023-01-01 10:02:00] 192.168.1.10 GET /about 200
[2023-01-01 10:03:15] 10.0.0.6 GET /admin 403
[2023-01-01 10:04:00] 192.168.1.11 GET /dashboard 200
[2023-01-01 10:05:00] 192.168.1.12 POST /api/order 500
EOF

echo "[2023-01-01 10:06:00] 192.168.1.12 POST /api/order 500" >> access.log
echo "[2023-01-01 10:07:00] 192.168.1.12 POST /api/order 500" >> access.log

# ==============================================================================
# 第二阶段：数据提取与分析 (正则与管道)
# ==============================================================================

# 3. 提取所有 4xx 或 5xx 的错误日志
# 提示：使用 grep 匹配状态码 (404, 403, 500 等)，可以使用正则 [45][0-9][0-9]
# TODO: 写出命令
grep -E "[45][0-9][0-9]" access.log

# 4. 统计每个 IP 的访问次数，并按次数降序排列
# 期望输出：
#   2 192.168.1.11
#   2 192.168.1.10
#   1 192.168.1.12
#   ...
# 提示：awk 提取 IP -> sort -> uniq -c -> sort -nr
# TODO: 写出命令
sort -k 2 | uniq - c| sort -k 1 -r | awk '{print $5}'

# 5. [正则挑战] 提取出日志中所有的 URL 路径（如 /index.html, /api/user）
# 提示：URL 在第 4 列，或者是利用正则匹配 / 开头的字符串
# TODO: 写出命令

# ==============================================================================
# 第三阶段：重命名与归档
# ==============================================================================

# 6. 将 access.log 重命名为 access_20230101.log
# 提示：使用 mv 命令
# TODO: 写出命令

# 7. 批量处理：假设当前目录下有多个 .log 文件 (可以先 touch error.log debug.log)
# 编写一个 for 循环，将所有 .log 文件移动到 ../backup 目录中 (需要先创建 backup 目录)
# TODO: 写出命令
find "*.log" . | awk '{for }'

# ==============================================================================
# 第四阶段：清理战场
# ==============================================================================
# 8. 删除 workspace 目录
# TODO: 写出命令
deldir workspace

